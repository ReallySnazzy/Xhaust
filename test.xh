// Comments are now supported
lol = 5
if (lol == 7-2)
{
    lol=10
    xd=20
    do Out(lol, xd)
    // String concatenation now supported
    do Out("Hel" + "lo, world!")
    // Now supporting console input
    name = do In("What's your name? ")
    do Out("Hello, " + name + "!")
    do Out(5*5*5)
}
if (lol == 9)
{
    do Out("This code won't happen...")
}
